# Linearly Transformed Sparse Probabilistic PCA (Demo)

Minimal implementation of an EM algorithm for linearly transformed sparse probabilistic PCA.

The notebook demonstrates:
- implementation of the sparse EM algorithm
- synthetic data generation
- recovery of the latent subspace
- comparison with a baseline linearly transformed PPCA model
